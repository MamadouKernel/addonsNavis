namespace EscaleReport.Web.Domain.Dispatch;

// CDC §9.1 : catégories suivies pour le module "Dispatch des autres engins".
public enum CategorieEngin { ReachStacker = 0, EmptyHandler = 1, Autre = 2 }
